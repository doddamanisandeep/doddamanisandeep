 echo "please enter the filename"
 read filename

 if [ -f $filename ]
 then
	 echo "file is already existed"
	 echo "file contents are"
	 cat $filename
 else
	 echo "file is not existed"
	 echo "file is being created and you can ener the contents"
	 vi $filename
 fi

